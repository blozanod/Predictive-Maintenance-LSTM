# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/

# Generated pipeline artifacts (Task 2.5: no results in the repo)
cache/
results/
*.npz

# Raw datasets live under one Data/ root (config.data_root). Only the small,
# committed C-MAPSS text files are tracked; large downloaded datasets a user
# drops in Data/ (XJTU-SY, N-CMAPSS, ...) are ignored.
/Data/*
!/Data/CMAPSSData/
