# TTM (ibm-granite/granite-timeseries-ttm-r2) via granite-tsfm (provides tsfm_public).
# granite-tsfm's `from transformers import PreTrainedModel` pulls transformers' object-
# detection loss -> torchvision, so torch and torchvision MUST be a matched pair or you
# get `operator torchvision::nms does not exist` (Colab's stock torchvision 0.26 wants
# torch 2.11, but granite-tsfm requires torch<2.11). Pin the granite-tsfm-compatible pair
# explicitly: torchvision 0.25.0 requires exactly torch 2.10.0 (verified on PyPI), and
# granite-tsfm accepts torch>=2.10,<2.11. On a fresh runtime this is a near no-op; in a
# polluted one it restores a consistent pair.
torch==2.10.0
torchvision==0.25.0
granite-tsfm>=0.2.22
