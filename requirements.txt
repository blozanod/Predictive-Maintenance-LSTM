# Runtime + test dependencies. The Colab notebook pins the same set in its setup
# cell. Versions below are lower bounds verified during development
# (Python 3.11). chronos-forecasting 2.x is required for Chronos2Pipeline.embed().

# --- core pipeline (needed for the CPU smoke tests) ---
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
torch>=2.1
matplotlib>=3.7           # src/plots.py figures (module-level import); Colab ships it, CI needs it
h5py>=3.10                # N-CMAPSS .h5 loader (src/datasets/ncmapss.py); tests write synthetic h5

# --- losses / baselines ---
coral-pytorch>=1.4        # CORN ordinal loss (Shi, Cao & Raschka 2021)
lightgbm>=4.0             # GBM baseline
sktime>=1.0              # MiniRocketMultivariate
numba>=0.59              # required by sktime's MiniRocket

# --- foundation model (GPU; Stage A only) ---
chronos-forecasting>=2.0.0   # official Chronos2Pipeline.embed()
safetensors>=0.4

# --- v2 TSFM backbones (GPU; Stage A only; IMPLEMENTATION_PLAN §2/§4.1) ---
# The four additional frozen embedders. Each is imported ONLY inside its embedder's
# _load_pipeline (never by the CPU smoke tests, which mock every backbone), and its
# exact API/version is confirmed by the Phase-1 integration spike before it joins a
# campaign; pin conservatively. package_versions() records these in run-metadata.
momentfm>=0.1.4              # MOMENT embedder (AutonLab/MOMENT-1-large)
uni2ts>=1.2.0               # Moirai-2 embedder (Salesforce/moirai-2)
timesfm>=1.3.0              # TimesFM 2.5 embedder (google/timesfm-2.5)
granite-tsfm>=0.2.22        # TTM embedder (ibm-granite/granite-timeseries-ttm-r2); provides tsfm_public

# --- test / coverage gate (IMPLEMENTATION_PLAN M0.1; CHANGES.md §32) ---
pytest>=7.0
pytest-cov>=4.0          # `pytest -q --cov=src --cov-branch`; .coveragerc pins fail_under=100
